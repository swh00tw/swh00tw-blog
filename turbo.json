{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["BACKEND_MONGODB_URI", "BACKEND_PAYLOAD_SECRET"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "lint-fix": {},
    "ts:check": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
